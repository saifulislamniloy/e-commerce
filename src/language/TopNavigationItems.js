class TopNavigationItems{
    static getHeader(mode){
        if(mode === 0)
            return "E-Commerce"
        else if( mode === 1)
            return "ই-বাণিজ্য"
    }
}
export default TopNavigationItems;